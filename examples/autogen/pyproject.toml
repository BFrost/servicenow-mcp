[project]
name = "service-now-agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "autogen-agentchat>=0.5.3",
    "autogen-ext[mcp,openai]>=0.5.3",
]
